export type ModalProps = {
  isModalOpen: boolean;
  setIsModalOpen: (open: boolean) => void;
};
export type PropsModal = {
  isModalCondominioOpen: boolean;
  setIsModalCondominioOpen: (open: boolean) => void;
};